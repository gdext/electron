{
  "name": "gdext",
  "version": "0.1.4",
  "description": "GDExt",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "npx electron-builder --dir  --publish never",
    "deploy": "npx electron-builder --win --publish never",
    "publish": "npx electron-builder --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gdext/electron.git"
  },
  "keywords": [],
  "author": "Ucrash",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/gdext/electron/issues"
  },
  "homepage": "https://github.com/gdext/electron#readme",
  "devDependencies": {
    "electron": "^11.0.3"
  },
  "dependencies": {
    "electron-updater": "^4.3.5",
    "tasklist": "^4.0.1"
  }
}
